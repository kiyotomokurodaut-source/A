# 黒田塾サイトのSEO：診断と作業記録

対象サイト：<https://kiyotomokuroda.netlify.app/>
調査日：2026年9月21日（公開版 2026-09-16-r12 を基準）

---

## 0. 先に読んでほしいこと：このリポジトリは本番に繋がっていません

調査時点で、このリポジトリ（`kiyotomokurodaut-source/A`）には `README.md` 1ファイル
しかありませんでした。一方で本番サイトは r12 が公開されています。
**本番の Netlify サイトは、このリポジトリからデプロイされていません。**

そのため、このブランチの変更は push しただけでは本番に反映されません。
反映するには、次のどちらかが必要です。

1. **Netlify をこのリポジトリに接続する（推奨）**
   Netlify の対象サイト → Site configuration → Build & deploy → Continuous deployment
   → Link repository で `kiyotomokurodaut-source/A` を選び、
   production branch をこのブランチ（またはマージ先の `main`）に設定します。
   `netlify.toml` に publish ディレクトリ（`public`）とビルドコマンドを書いてあるので、
   追加設定は不要です。
2. **`public/` の中身を手元に落として、これまでと同じ方法でデプロイする**
   ドラッグ＆ドロップや Netlify CLI を使っている場合は、`public/` をそのまま公開ディレクトリ
   として扱えます。

接続前に、いまの本番ファイルがこのリポジトリの `public/` と一致しているか確認してください。
`public/` は調査日の本番HTMLをそのまま取り込んだうえで変更を重ねたものですが、
その後に本番側だけで更新が入っていた場合は差分が消えます。

---

## 1. 診断：すでに十分できていたこと

順位が付いていない原因を「内部SEOの不備」と見るのは、このサイトでは誤りでした。
調査した範囲では、内部対策はすでにかなり作り込まれていました。

| 項目 | 状態 |
| --- | --- |
| `canonical` | 全ページに正しいURLで設定済み |
| `robots` メタ | 全ページ設定済み。404 のみ `noindex, follow` |
| 構造化データ | WebPage / Article / Service / OfferCatalog / BreadcrumbList / Person などを @graph で記述済み |
| パンくず | HTMLとJSON-LDの両方に存在 |
| `title` / `description` | 全ページ固有。重複なし。長さも適切 |
| 見出し階層 | `h1` は各ページ1つ。レベル飛びなし |
| `sitemap.xml` / `robots.txt` | 設置済み。相互参照あり |
| URL正規化 | `/pricing` → `/pricing/`、`/pricing/index.html` → `/pricing/`、大文字も301。重複URLなし |
| 配信 | Brotli有効、`/assets/*` は `immutable` で1年キャッシュ |
| 表示速度 | TTFB 約0.32秒、HTML 35KB、CSS 41KB（圧縮後はさらに小）、Webフォント読み込みなし |
| 画像 | `width`/`height` 指定あり（CLS対策済み）、`fetchpriority`、WebPの`srcset`あり |
| 内部リンク | リンク切れ・アンカー切れ・孤立ページなし |
| 本文の質 | 誇張表現を避け、「指導経験」と「合格実績」を明確に区別。E-E-A-T上は有利 |

**つまり、内部対策で稼げる残りの伸びは大きくありません。**
残っていた本当の課題は次の3つです。

---

## 2. 本当の課題

### 課題A：インデックスされていない可能性が高い

`site:` に相当する検索と、サイト固有の語（`黒田清友`、ドメイン名）での検索で、
このサイトのページが確認できませんでした。
`黒田清友` で出てくるのは LinkedIn のプロフィールだけです。

内部対策が整っていても、インデックスされていなければ順位は付きません。
**これが最優先の課題です。** 対処は「4. サイト所有者しかできない作業」に書きました。

> 注：確認に使った検索は米国リージョンの検索APIで、Google日本の結果とは一致しません。
> 正確な状況は Google Search Console の「ページ」レポートで確認してください。

### 課題B：「黒田塾」は競合の多いブランド名

`黒田塾` で検索すると、すでに次のサイトが上位に存在します。

- <https://kuroda-juku.jp/> — 「オンライン黒田塾」
- <https://www.kuroda-juku-mito.jp/> — 水戸の黒田学習塾（塾ナビ・塾選・アメ塾にも掲載）
- 大阪の黒田進学ゼミ、黒田時習塾、note の講座「黒田塾」など

同名の事業者が複数あり、そのうち何社かは比較サイト（塾ナビ／塾選／アメ塾）に
掲載されています。被リンクと引用の蓄積で不利なため、
**`黒田塾` 単体で1位を取る戦略は、費用対効果が悪い**と判断しました。

取りに行くべきブランド語は **`黒田清友`** です。同名の事業者がおらず、
本人名＋実績で一意に紐づけられます。
サイト名を変える必要はありませんが、外部での名乗り方（プロフィール、SNS、
被リンクのアンカーテキスト）は「黒田清友」を主、「黒田塾」を従にするのが有利です。

### 課題C：ヘッドキーワードは比較サイトが占有

`オンライン家庭教師`、`家庭教師 料金 相場` のような検索結果は、
塾ナビ・塾選・アメ塾・jyukumado などの比較メディアと大手企業が占めています。
個人サイトが正面から競合できる領域ではありません。

現実的に勝てるのは、**具体的な悩みの語（ロングテール）** です。
このサイトの学習ガイドはその方向で作られており、判断は正しいです。
したがって今回の施策も「ページ数と網羅性を増やす」方向に寄せました。

---

## 3. このブランチで実施した変更

### 3-1. 新規ページ（4本）

いずれもサイト既存の記述だけを根拠にし、公表されていない条件は
「契約前に個別確認」と明示しています。事実の追加・創作はしていません。

| URL | 狙う検索意図 |
| --- | --- |
| `/faq/` | 料金・科目・受講方法・相談に関する21問。「月4セットとは」「学習管理だけ頼める？」など確認系の検索を広く拾う |
| `/study-guides/kakomon-start-timing/` | 「過去問 いつから」。月で決めず3条件で判断する方法、解いたあとの手順 |
| `/study-guides/common-test-english-time/` | 「共通テスト英語 時間が足りない」。原因を4つに分けて記録する手順（既存の英語長文ページと連携） |
| `/study-guides/online-tutoring-vs-agency/` | 「オンライン家庭教師 個人契約」。会社経由との違いと契約前チェック8項目。自分が個人側であることを開示し、会社経由が向く場合も併記 |

### 3-2. 構造化データ

- **FAQPage を3ページに追加**（`/pricing/` 6問、`/contact/` 3問、`/faq/` 21問、計30問）。
  `/pricing/` と `/contact/` はもともと `<details>` で本物のQ&Aを持っていたのに、
  機械可読な形がありませんでした。
  ただし **Googleは2023年8月にFAQリッチリザルトを政府・医療系サイトに限定した**ため、
  検索結果の見た目は変わりません。目的は、パーサーと生成AI検索が回答を引用しやすくすることです。
- **例題の解答は FAQPage にしていません。** `<details>` を使っていても、
  「解答・和訳・考え方を読む」はFAQではなく演習の解答です。
  FAQとして記述するとページ内容を偽って伝えることになります。
- **Article の `author` を組織から本人（Person）へ変更。** 学習ガイドは
  学歴を明示した個人が書いています。組織名義よりE-E-A-T上有利です。
- **`dateModified` を全記事に追加**（2ページで欠落、`/study-guides/` 系は全ページで
  `last-modified` メタと同期）。
- **`#website` / `#organization` / `#teacher` を全ページに保証。**
  `/cases/` だけ組織ノードが欠けていました。
- 組織に `areaServed`（日本）、`availableLanguage`（ja）を追加。

### 3-3. SNSシェアカード

全ページが 900×1200 の縦長ポートレートを `twitter:card=summary` で参照していました。
シェアカードは 1.91:1 なので、LINEやXに貼ると顔の一部が切り取られ、文字が出ません。

- `tools/make_og_cards.py` で **ページ別 1200×630 カードを21枚生成**
  （各ページの見出しと要点入り、フラット配色、1枚あたり約15KB）。
- トップと404には写真入りの `og-card.png` を使用。
- `twitter:card` を `summary_large_image` に変更。
  `og:image:type` / `width` / `height` / `alt` も明記。

### 3-4. クロールと発見性

- **`llms.txt` を追加。** どのURLがどの質問に答えるかを1リクエストで示します。
  生成AI検索が内容を取り違えにくくなります。
- **`feed.xml`（RSS）を追加。** 新しいガイドの公開を、クロール待ちなしで拾えるようにします。
  全ページの `<head>` から `rel="alternate"` で参照。
- **`robots.txt` を再生成。** 主要AIクローラーを明示的に許可し、
  記入シートのクエリ文字列（`?print=` など）はクロール対象から外しました
  （同じ本文がパラメータ違いで重複するため）。
- **`sitemap.xml` を再生成。** `lastmod` を各ページの `last-modified` メタと同期し、
  画像サイトマップ（`image:image`）を追加。更新順に並べています。

### 3-5. 内部リンク

- `/faq/` をフッターとモバイルメニュー（全ページ）、トップの導線、
  `/site-map/` に追加。
- 新しいガイド3本を、`/study-guides/` ハブのカードとJSON-LDの `ItemList`、
  `/site-map/`、関連する指導案内ページ（大学受験・中学受験・オンライン家庭教師・
  学習管理）から相互リンク。
- `/cases/` を加筆（774字 → 1,671字）。事例の三段階の区別（指導経験／確認できている結果／
  非公開情報）を明文化し、相談の入口を整理しました。事例の事実は追加していません。
- `/404.html` の description が43字と短かったので98字に。`/faq/` への導線も追加。

### 3-6. 配信設定（`netlify.toml`）

- セキュリティヘッダ（`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`）。
- 画像・PDFのキャッシュ指定、生成テキストファイルの `must-revalidate`。
- **Deploy Preview とブランチデプロイに `X-Robots-Tag: noindex`。**
  Netlifyはプレビューを別ホスト名で公開するため、放置すると本番と重複した内容が
  インデックスされる可能性があります。
- `public/_redirects` に想定される打ち間違いURLの301を追加。

### 3-7. 回帰を防ぐ仕組み

`build.py` は生成ファイルを書き出すだけでなく、次を検査して**失敗したらデプロイを止めます**
（`netlify.toml` の build command が `--check` 付き）。標準ライブラリのみで動くので、
pip の失敗でデプロイが落ちることはありません。

- `title` / `description` の有無・長さ・**サイト内での重複**
- `h1` がちょうど1つ
- `canonical` が自URLと一致
- OG/Twitterの必須項目、`summary_large_image`、画像ファイルの実在
- `last-modified` の形式と未来日付でないこと、Article の `dateModified` との一致
- JSON-LDがパースできること、共有エンティティ3ノードの存在
- `img` の `alt` と `width`/`height`
- **内部リンクの到達性（アンカー `#id` まで）**
- **孤立ページの検出**（どこからもリンクされていないページ）

実際にこの検査で、調査時点の48件の不整合を検出し、すべて解消しました。

---

## 4. サイト所有者しかできない作業（優先順）

コードで解決できない部分です。**ここが順位に一番効きます。**

### 優先度1：インデックスさせる（今日やる）

1. Google Search Console を開く（`google-site-verification` メタは既に入っているので
   所有権確認は済んでいるはずです）。
2. 「サイトマップ」に `https://kiyotomokuroda.netlify.app/sitemap.xml` を送信。
3. 「URL検査」でトップページを検査し、未登録なら**インデックス登録をリクエスト**。
   主要ページ（`/pricing/`、`/online-tutoring/`、`/faq/`、`/study-guides/`）も同様に。
4. 「ページ」レポートで除外理由を確認。
   「クロール済み - インデックス未登録」が多い場合は、原因は技術ではなく
   **需要と外部評価の不足**です。優先度2へ。
5. Bing Webmaster Tools にも登録し、サイトマップを送信。
   （Yahoo! JAPANの検索はGoogleのインデックスを使うため、
   **Yahoo対策としてGoogleとは別に何かをする必要はありません。**
   Bingは別インデックスなので登録の価値があります。）

### 優先度2：外部からの言及を作る

新規ドメインで被リンクがゼロだと、内部対策がどれだけ整っていても上がりません。
広告ではなく、実在する所に載せるところから始めます。

- LINE公式アカウントのプロフィールにサイトURLを記載。
- 本人名義のSNS／プロフィールサービスにURLを記載し、
  **アンカーテキストは「黒田清友」**を主に使う。
- 塾比較サイト（塾ナビ、塾選、アメ塾など）への掲載を検討。
  競合の「黒田学習塾」がここから流入を得ています。
- 掲載や言及が取れたら、`/about/` の Person 構造化データに `sameAs` を追加します
  （本人確認できるプロフィールURLのみ。未確認のURLを入れると誤った同一視を招くので、
  **今回は意図的に空のままにしてあります**）。

### 優先度3：独自ドメイン

`*.netlify.app` でも順位は付きますが、次の理由で独自ドメインが有利です。

- 事業者としての信頼性（料金を扱うページでは影響が大きい）
- Search Console のドメインプロパティが使える
- 将来サーバーを移しても評価を引き継げる

移す場合は Netlify 側でカスタムドメインを設定し、
`netlify.app` のURLから **301リダイレクト**を必ず張ってください。
リダイレクトなしで切り替えると、いま積んだ評価がゼロに戻ります。
移行時は、このリポジトリの `SITE` 定数（`build.py` と `tools/*.py`）と
各ページの `canonical` / `og:url` を一括置換する必要があります。

### 優先度4：コンテンツを増やし続ける

ロングテール戦略はページ数が効きます。1本あたり2,000〜4,000字を目安に、
**実際に相談で聞かれたこと**を題材にするのが最も効率的です。候補：

- 中学受験の志望校の決め方／併願の組み方
- 共通テスト数学で時間が足りないとき（英語版の姉妹記事）
- 英検・TEAPなど外部試験の使い方
- 部活と受験勉強の両立（使える時間の計算）
- 保護者の関わり方（どこまで管理するか）
- 不登校からの大学受験（実際の対応範囲の範囲内で）

書いたら `python3 tools/normalize_seo.py && python3 build.py --check` を実行すれば、
head・構造化データ・サイトマップ・RSS・llms.txt が揃います。
シェアカードは `tools/make_og_cards.py` の `CARDS` に1行足して再生成します。

---

## 5. やらなかったこと（意図的に）

- **`sameAs` の追加。** `黒田清友` の LinkedIn プロフィールが見つかりましたが、
  本人のものだと確認できていません。誤った同一視は構造化データの信頼を落とすため、
  本人確認後に追加してください。
- **口コミ・評価（`Review` / `AggregateRating`）の構造化データ。**
  サイトに実在のレビューがありません。無い評価を記述するのは
  Googleのガイドライン違反で、手動対策の対象です。
- **例題の解答を FAQPage にする。** 前述のとおり、内容を偽って伝えることになります。
- **クリティカルCSSのインライン化などの速度改善。** TTFB 0.32秒、Webフォントなし、
  Brotli有効で、すでに速いため効果が小さいと判断しました。
- **`黒田塾` 単体での上位表示。** 課題Bのとおり、費用対効果が悪いと判断しました。

---

## 6. 計測

順位を直接追うより、次を月1回見るほうが判断に使えます。

1. **Search Console →「ページ」**：インデックス済みページ数。
   まずこれが22に近づくことが目標です。
2. **Search Console →「検索結果」→ クエリ**：表示回数が付き始めた語。
   ここに出てきた語が、次に書く記事の題材になります。
3. **`黒田清友` での検索順位**：ブランド語として取れているか。
4. **LINEの相談件数**：最終的な指標。検索順位はここに繋がって初めて意味があります。

インデックスされてから順位が動き始めるまでは、通常数週間から数か月かかります。
1〜2週間で判断せず、月次で見てください。
