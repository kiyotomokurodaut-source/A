# 黒田塾サイトのSEO：診断と作業記録

本番ホスト（予定）：<https://kiyotomokuroda.pages.dev/>
調査日：2026年9月21日

---

## 0. 最優先：`kiyotomokuroda.pages.dev` を作ってください

**このドメインは、現時点でDNSが引けません。** プロキシ障害ではなく名前解決の失敗です。

```
kiyotomokuroda.pages.dev                          → 解決できません（NXDOMAIN）
lively-cake-2d04.kiyotomokurodaut.workers.dev     → 104.21.39.218
kiyotomokuroda.netlify.app                        → 98.84.224.111
```

ところが `workers.dev` で公開されている内容は、`canonical`・`og:url`・`sitemap.xml`・
`robots.txt` のすべてが `kiyotomokuroda.pages.dev` を指しています。

**canonicalが存在しないホストを指すと、Googleはそのページをインデックスから落とします。**
つまり r15 で追加された東大対策8ページは、公開されていても検索流入がゼロのままです。

Cloudflare で Pages プロジェクトを作り、**プロジェクト名を `kiyotomokuroda`** にしてください
（プロジェクト名がそのまま `<名前>.pages.dev` になります）。
このリポジトリを繋げば、`netlify.toml` と同じ内容を `public/_headers` と
`public/_redirects` に用意してあるので追加設定は不要です。

- Build command： `python3 build.py --check`（未対応なら空でも動きます。生成ファイルはコミット済み）
- Build output directory： `public`
- Production branch： `main`

### 3ホストの整理（重要）

同じ内容が複数ホストで配信されると評価が分散し、どれも上がりません。1つに寄せます。

| ホスト | いまの状態 | やること |
| --- | --- | --- |
| `kiyotomokuroda.pages.dev` | **存在しない** | **作る。ここを本番にする** |
| `...workers.dev` | r15を配信中。canonicalは死んだホストを指す | Pages公開後、**301でpages.devへ**送るか、Workerを削除 |
| `kiyotomokuroda.netlify.app` | r12（旧内容）を配信中・インデックス可 | `netlify.toml` に全パス301を入れてあります。Netlifyをこのリポジトリに繋ぐか、同じ301を手で設定 |

`workers.dev` と `netlify.app` は、消すより **301で送る**ほうが有利です。
旧URLが得た評価を新URLへ引き継げます。

### 反映確認

```sh
curl -s -o /dev/null -w '%{http_code}\n' https://kiyotomokuroda.pages.dev/faq/   # 200 なら反映済み
curl -s https://kiyotomokuroda.pages.dev/ | grep -o 'summary_large_image'         # 出れば新しい head
curl -sI https://kiyotomokuroda.pages.dev/ | grep -i x-robots-tag                 # 何も出ないのが正しい
curl -s https://kiyotomokuroda.pages.dev/sitemap.xml | grep -c '<loc>'            # 29 になる
```

> Cloudflare Pages はプレビューデプロイに自動で `X-Robots-Tag: noindex` を付けるため、
> ブランチプレビューが本番と重複してインデックスされる心配はありません。

---

## 1. 診断：内部SEOはすでに十分できていた

順位が付かない原因を「内部SEOの不備」と見るのは、このサイトでは誤りでした。

| 項目 | 状態 |
| --- | --- |
| `canonical` | 全ページ設定済み |
| `robots` メタ | 全ページ設定済み。404のみ `noindex, follow` |
| 構造化データ | WebPage / Article / Service / OfferCatalog / BreadcrumbList / Person を @graph で記述済み |
| パンくず | HTMLとJSON-LDの両方に存在 |
| `title` / `description` | 全ページ固有。重複なし |
| 見出し階層 | `h1` は各ページ1つ。レベル飛びなし |
| URL正規化 | `/pricing` → `/pricing/` などを301。重複URLなし |
| 配信 | Brotli有効、`/assets/*` はハッシュ付きで長期キャッシュ |
| 表示速度 | TTFB 約0.32秒、Webフォント読み込みなし |
| 画像 | `width`/`height` 指定あり（CLS対策済み）、WebPの`srcset`あり |
| 本文の質 | 誇張を避け、「指導経験」と「合格実績」を明確に区別。E-E-A-T上は有利 |

**内部対策で稼げる残りの伸びは大きくありません。** 本当の課題は次の3つです。

---

## 2. 本当の課題

### 課題A：インデックスされていない

サイト固有の語（`黒田清友`、ドメイン名）での検索で、このサイトのページが確認できませんでした。
`黒田清友` で出るのは LinkedIn のプロフィールだけです。
内部対策が整っていても、インデックスされなければ順位は付きません。

第0章のホスト整理が、そのまま最優先の対処です。canonicalが死んでいる限り、
他に何をしても載りません。

> 注：確認に使った検索は米国リージョンの検索APIで、Google日本の結果とは一致しません。
> 正確な状況は Google Search Console の「ページ」レポートで確認してください。

### 課題B：「黒田塾」は競合の多いブランド名

`黒田塾` で検索すると、すでに次のサイトが上位にあります。

- <https://kuroda-juku.jp/> — 「オンライン黒田塾」
- <https://www.kuroda-juku-mito.jp/> — 水戸の黒田学習塾（塾ナビ・塾選・アメ塾にも掲載）
- 大阪の黒田進学ゼミ、黒田時習塾、note の講座「黒田塾」など

同名事業者が複数あり、うち何社かは比較サイトに掲載されています。
被リンクと引用の蓄積で不利なため、**`黒田塾` 単体で1位を取る戦略は費用対効果が悪い**
と判断しました。

取りに行くべきブランド語は **`黒田清友`** です。同名の事業者がおらず、本人名で一意に紐づきます。
サイト名を変える必要はありませんが、外部での名乗り方（プロフィール、SNS、被リンクの
アンカーテキスト）は「黒田清友」を主、「黒田塾」を従にするのが有利です。

### 課題C：ヘッドキーワードは比較サイトが占有

`オンライン家庭教師`、`家庭教師 料金 相場` の検索結果は、塾ナビ・塾選・アメ塾・
jyukumado などの比較メディアと大手企業が占めています。個人サイトが正面から competing
できる領域ではありません。

現実的に勝てるのは**具体的な悩みの語（ロングテール）**です。
このサイトの学習ガイドはその方向で作られており、判断は正しいです。
r15 の東大対策クラスタ（`/todai-juku/`、`todai-*` 6本）も同じ方向で、良い設計です。

---

## 3. 実施した変更

### 3-1. r15 との統合

作業の途中で、Cloudflare への移行（r15）が並行して進んでいることが分かりました。
このリポジトリは r12 を基に作業していたため、まず内容を突き合わせました。

- r15 は**共有18ページ中17ページを変更**していました（トップ +782字、contact +360字、
  study-guides +325字、university-exam +256字など）。
  **r15 を内容のベースに採用**しました。r12ベースのまま進めると、これらが失われます。
- r15 にしかない8ページ（`/juku/`、`/todai-juku/`、`todai-*` 6本）をそのまま取り込みました。
- 私が書いた4ページのうち**3ページを残し、1ページは破棄**しました（次項）。
- r15 で **LINEのURLが `lin.ee/YXdwMqY` に変更**され（旧 `8euXQgoq`）、CTA文言も
  「無料受験戦略診断」「LINE追加で無料特典」に変わっていました。
  追加ページも新しいURL・文言・ヘッダ構造（`r15-toc` / `r15-related` / `r15-note` /
  `byline` / `btn btn-dark`）に合わせて作り直しました。

結果、**29ページ（+404）**の一本化されたサイトになりました。

### 3-2. 追加したページ（3本）

サイト既存の記述だけを根拠にし、公表されていない条件は「契約前に個別確認」と明示しています。
事実の追加・創作はしていません。

| URL | 狙う検索意図 |
| --- | --- |
| `/faq/` | 料金・科目・受講方法・無料受験戦略診断の25問。「月4セットとは」「学習管理だけ頼める？」「90日キットとは」など確認系を広く拾う |
| `/study-guides/kakomon-start-timing/` | 「過去問 いつから」。月で決めず3条件で判断する方法。r15の `todai-kakomon-review`（復習法）と補完関係で、中学受験も対象 |
| `/study-guides/online-tutoring-vs-agency/` | 「オンライン家庭教師 個人契約」。会社経由との違いと契約前チェック8項目。自分が個人側であることを開示し、会社経由が向く場合も併記 |

### 3-3. 破棄した1ページ（重要な判断）

`/study-guides/common-test-english-time/`（共通テスト英語の時間不足）を**破棄しました。**

r15 の `/study-guides/kyotsu-english-time/` が同じ検索意図を狙っており、
しかも**内容が上位互換**でした（止まる作業を5分類 vs 私の4分類、自作問題3問と全文和訳、
「時間を追加したら解けたか」による診断、記録ツール）。

2本残すとキーワードが共食いし、私の4分類を先方のページに足すと、
既にある5分類と重複して**ページの質が下がります**。
padding より削除が正しいと判断しました。`_redirects` に301だけ入れてあります。

### 3-4. 構造化データ

- **FAQPage を4ページに追加（合計38問）。**
  `/pricing/` 6問、`/contact/` 3問、`/todai-juku/` 4問、`/faq/` 25問。
  前3つはもともと `<details>` で本物のQ&Aを持ちながら、機械可読な形がありませんでした。
  ただし **Googleは2023年8月にFAQリッチリザルトを政府・医療系サイトに限定した**ため、
  検索結果の見た目は変わりません。目的はパーサーと生成AI検索が回答を引用しやすくすることです。
- **例題の解答は FAQPage にしていません。** `<details>` を使っていても
  「解答・和訳・考え方を読む」はFAQではなく演習の解答です。
  FAQとして記述すると、ページ内容を偽って伝えることになります。
- **Article の `author` を組織から本人（Person）へ。** 学習ガイドは学歴を明示した個人が
  書いています。組織名義よりE-E-A-T上有利です。
- **`dateModified` を全記事で `last-modified` メタと同期。**
- **`#website` / `#organization` / `#teacher` を全29ページに保証。**
- 組織に `areaServed`（日本）、`availableLanguage`（ja）を追加。

### 3-5. SNSシェアカード

全ページが 900×1200 の縦長ポートレートを `twitter:card=summary` で参照していました。
シェアカードは 1.91:1 なので、LINEやXに貼ると顔の一部が切り取られ、文字が出ません。

- `tools/make_og_cards.py` で **ページ別 1200×630 カードを28枚生成**（`/og/` 配下、
  各ページの見出しと要点入り、1枚約15KB、合計約440KB）。
- トップと404には写真入りの `og-card.png`。
- `twitter:card` を `summary_large_image` に変更。`og:image:type` / `width` / `height` /
  `alt` も明記。
- **カードは `/assets/` ではなく `/og/` に置いています。** `/assets/*` は
  `Cache-Control: immutable` で1年キャッシュしますが、ハッシュ付きのファイルだから
  それが成立します。カードは固定名なので、`/assets/` に置くと文面を直して再生成しても
  1年間古い画像が返り続けます。

### 3-6. クロールと発見性

- **`llms.txt`** — どのURLがどの質問に答えるかを1リクエストで示します。
- **`feed.xml`（RSS、15件）** — 新しいガイドをクロール待ちなしで拾えるようにします。
  全ページの `<head>` から `rel="alternate"` で参照。
- **`robots.txt`** — 主要AIクローラーを明示的に許可し、記入シートのクエリ文字列
  （`?print=` など）はクロール対象から外しました（同じ本文がパラメータ違いで重複するため）。
- **`sitemap.xml`（29件）** — `lastmod` を各ページの `last-modified` メタと同期し、
  画像サイトマップ（`image:image`）を追加。更新順に並べています。

### 3-7. 内部リンク

- `/faq/` をフッターとモバイルメニュー（全ページ）、トップ、`/site-map/` に追加。
- 追加ガイド2本を、`/study-guides/`、`/site-map/`、関連する指導案内ページ
  （大学受験・中学受験・オンライン家庭教師・学習管理・塾・東大受験）から相互リンク。
- `/404.html` の description が43字と短かったので98字に。`/faq/` への導線も追加。

### 3-8. 配信設定

`public/_headers` と `public/_redirects`（Cloudflare Pages と Netlify で共通形式）：

- セキュリティヘッダ（`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`、
  `X-Content-Type-Options`）。
- `/assets/*` は1年immutable、`/og/*` と `og-card.png` は1日再検証、
  生成テキストは `must-revalidate`。
- 打ち間違い・旧URLからの301（`/todai` → `/todai-juku/` など）。
- **拡張子グロブ（`/*.webp` など）は使っていません。** Netlify も Cloudflare も
  ヘッダ規則はパス単位でマッチするため、確実に効きません。ルート直下の画像は明示パスで列挙。

`netlify.toml` は、旧ホスト `kiyotomokuroda.netlify.app` を全パス301で
`pages.dev` へ送るためだけの設定に変えました。

### 3-9. 回帰を防ぐ仕組み

`build.py` は生成ファイルを書き出すだけでなく、次を検査して**失敗したらデプロイを止めます**。
標準ライブラリのみで動くので、pipの失敗でデプロイが落ちることはありません。

- `title` / `description` の有無・長さ・**サイト内での重複**
- `h1` がちょうど1つ
- `canonical` が自URLと一致
- OG/Twitterの必須項目、`summary_large_image`、画像ファイルの実在
- `last-modified` の形式と未来日付でないこと、Article の `dateModified` との一致
- JSON-LDがパースできること、共有エンティティ3ノードの存在
- `img` の `alt` と `width`/`height`
- **内部リンクの到達性（アンカー `#id` まで）**
- **孤立ページの検出**（どこからもリンクされていないページ）

調査時点の不整合48件をこの検査で検出し、すべて解消しました。
全30ページをヘッドレスブラウザで描画確認済み（コンソールエラー0、横スクロール0、
`h1` は各1つ、og:image 29パスすべて解決）。

---

## 4. サイト所有者しかできない作業（優先順）

### 優先度0：ホストを1つにする（第0章）

**これが終わるまで、他の施策は効きません。** canonicalが存在しないホストを指している限り、
r15の8ページは検索結果に出ません。

### 優先度1：インデックスさせる

1. Google Search Console に **`kiyotomokuroda.pages.dev` のプロパティを追加**
   （既存の `netlify.app` プロパティとは別物です）。
2. 「サイトマップ」に `https://kiyotomokuroda.pages.dev/sitemap.xml` を送信。
3. 「URL検査」でトップページを検査し、未登録なら**インデックス登録をリクエスト**。
   主要ページ（`/pricing/`、`/todai-juku/`、`/faq/`、`/study-guides/`）も同様に。
4. 「ページ」レポートで除外理由を確認。
   「クロール済み - インデックス未登録」が多い場合、原因は技術ではなく
   **需要と外部評価の不足**です。優先度2へ。
5. 旧 `netlify.app` プロパティは残し、301が認識されるのを確認します。
6. Bing Webmaster Tools にも登録し、サイトマップを送信。
   （Yahoo! JAPANの検索はGoogleのインデックスを使うため、
   **Yahoo対策としてGoogleとは別に何かをする必要はありません。**
   Bingは別インデックスなので登録の価値があります。）

### 優先度2：外部からの言及を作る

新規ドメインで被リンクがゼロだと、内部対策がどれだけ整っていても上がりません。

- LINE公式アカウントのプロフィールにサイトURLを記載。
- 本人名義のSNS／プロフィールにURLを記載し、**アンカーテキストは「黒田清友」**を主に。
- 塾比較サイト（塾ナビ、塾選、アメ塾など）への掲載を検討。
  競合の「黒田学習塾」はここから流入を得ています。
- 掲載が取れたら、`/about/` の Person 構造化データに `sameAs` を追加します
  （本人確認できるURLのみ。未確認のURLは誤った同一視を招くので、
  **今回は意図的に空のままにしてあります**）。

### 優先度3：独自ドメイン

`*.pages.dev` でも順位は付きますが、独自ドメインが有利です。

- 事業者としての信頼性（料金を扱うページでは影響が大きい）
- Search Console のドメインプロパティが使える
- 将来サーバーを移しても評価を引き継げる

移す場合は Cloudflare 側でカスタムドメインを設定し、`pages.dev` から
**301リダイレクト**を必ず張ってください。移行時は、このリポジトリの `SITE` 定数
（`build.py` と `tools/*.py`）と各ページの `canonical` / `og:url` を一括置換します。

**ホストを変えるたびに評価はリセットに近づきます。** これで3回目（netlify → workers →
pages）なので、独自ドメインに移すならこれを最後にしてください。

### 優先度4：コンテンツを増やし続ける

ロングテール戦略はページ数が効きます。**実際に相談で聞かれたこと**を題材にするのが
最も効率的です。r15の東大クラスタは良い設計なので、同じ形で他の志望校帯にも広げられます。

候補：中学受験の志望校の決め方／併願の組み方、共通テスト数学で時間が足りないとき、
英検・TEAPなど外部試験の使い方、部活と受験勉強の両立、保護者の関わり方、
不登校からの大学受験（実際の対応範囲の範囲内で）。

書いたら次を実行すれば、head・構造化データ・サイトマップ・RSS・llms.txt が揃います。

```sh
python3 tools/normalize_seo.py   # LASTMOD に URL を追加してから
python3 tools/make_og_cards.py   # CARDS に文面を1行追加してから
python3 build.py --check
```

---

## 5. やらなかったこと（意図的に）

- **`sameAs` の追加。** `黒田清友` の LinkedIn プロフィールが見つかりましたが、
  本人のものだと確認できていません。誤った同一視は構造化データの信頼を落とすため、
  本人確認後に追加してください。
- **口コミ・評価（`Review` / `AggregateRating`）の構造化データ。**
  サイトに実在のレビューがありません。無い評価を記述するのは
  Googleのガイドライン違反で、手動対策の対象です。
- **例題の解答を FAQPage にする。** 内容を偽って伝えることになります。
- **`/study-guides/common-test-english-time/` の統合。** 3-3のとおり、
  r15側が上位互換なので破棄しました。
- **クリティカルCSSのインライン化などの速度改善。** TTFB 0.32秒、Webフォントなし、
  Brotli有効で、すでに速いため効果が小さいと判断しました。
- **`黒田塾` 単体での上位表示。** 課題Bのとおり、費用対効果が悪いと判断しました。

---

## 6. 計測

順位を直接追うより、次を月1回見るほうが判断に使えます。

1. **Search Console →「ページ」**：インデックス済みページ数。まず29に近づくことが目標です。
2. **Search Console →「検索結果」→ クエリ**：表示回数が付き始めた語。
   ここに出た語が、次に書く記事の題材になります。
3. **`黒田清友` での検索順位**：ブランド語として取れているか。
4. **LINEの相談件数**：最終的な指標。検索順位はここに繋がって初めて意味があります。

インデックスされてから順位が動き始めるまでは、通常数週間から数か月かかります。
1〜2週間で判断せず、月次で見てください。
